use super::ListenCollection;

impl ListenCollection {
    // pub async fn get_recording_listens(&self, id: &PrimaryMBID<Recording>) -> Self {
    //     stream::iter(self.clone().into_iter()).filter(|listen| listen.get_mapped_primary_mbid())
    // }
}
